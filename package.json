{
  "name": "app-prueba",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "deploy": "npm install && npm run build && npm run start -p 8080",
    "dev": "next dev -p 80",
    "build": "next build",
    "start": "npm install && npm run build && next start -p 80",
    "lint": "next lint"
  },
  "dependencies": {
    "react": "^18",
    "react-dom": "^18",
    "next": "14.2.5"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "postcss": "^8",
    "tailwindcss": "^3.4.1",
    "eslint": "^8",
    "eslint-config-next": "14.2.5"
  }
}